import React from "react";
import styles from "./styles.css";

export const Header = () => (
  <nav className={styles.HeaderContainer}>
    <h2 className={styles.Title}>Crisis Team Builder</h2>
  </nav>
);
